import { useEffect, useState } from 'react';
import { Activity, BarChart3, BookOpen, Brain, RefreshCw, Target, TrendingUp, Zap } from 'lucide-react';
import { Bar, BarChart, CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import api, { getApiError } from '../../services/api';
import ProgressBar from '../../components/ui/ProgressBar';

export default function AnalyticsPage() {
  const [data, setData] = useState(null); const [error, setError] = useState('');
  function load() { api.get('/analytics/overview').then(({ data: response }) => setData(response.data)).catch((requestError) => setError(getApiError(requestError, 'Analytics could not be loaded.'))); }
  useEffect(load, []);
  if (!data) return <div className="space-y-6"><div className="skeleton h-16 w-72" /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{[1, 2, 3, 4].map((item) => <div key={item} className="skeleton h-32 rounded-2xl" />)}</div>{error && <div className="rounded-xl border border-rose/20 bg-rose/10 p-4 text-sm text-rose">{error} <button type="button" onClick={load} className="ml-2 underline">Retry</button></div>}</div>;
  const stats = [{ label: 'Goal completion', value: `${data.summary.goalCompletion}%`, icon: Target, tone: 'moss' }, { label: 'Focus time', value: `${data.summary.focusHours}h`, icon: Brain, tone: 'amber' }, { label: 'Skill progress', value: `${data.summary.skillProgress}%`, icon: TrendingUp, tone: 'moss' }, { label: 'Book progress', value: `${data.summary.bookProgress}%`, icon: BookOpen, tone: 'amber' }];
  return <div className="animate-page space-y-7"><section className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-moss">Analytics</p><h1 className="mt-3 font-display text-4xl font-semibold tracking-[-0.03em] text-cream sm:text-5xl">See the patterns behind your progress.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted">A simple read on the habits, tasks, and projects you have chosen to make visible.</p></div><button type="button" onClick={load} className="button-secondary self-start md:self-auto"><RefreshCw size={15} /> Refresh</button></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(({ label, value, icon: Icon, tone }) => <div key={label} className="rounded-2xl border border-line bg-panel p-5 shadow-glow"><div className="flex items-start justify-between"><span className={`flex h-9 w-9 items-center justify-center rounded-xl bg-ink ${tone === 'amber' ? 'text-amber' : 'text-moss'}`}><Icon size={17} /></span><Activity size={15} className="text-muted" /></div><p className="mt-6 text-sm text-muted">{label}</p><p className="mt-1 font-display text-3xl font-semibold text-cream">{value}</p></div>)}</section><section className="grid gap-5 xl:grid-cols-[1.2fr_0.8fr]"><div className="rounded-2xl border border-line bg-panel p-5 shadow-glow sm:p-6"><div className="flex items-start justify-between"><div><p className="text-xs uppercase tracking-[0.14em] text-muted">Daily productivity</p><h2 className="mt-2 font-display text-xl font-semibold text-cream">The last two weeks.</h2></div><BarChart3 size={18} className="text-moss" /></div><div className="mt-7 h-64"><ResponsiveContainer width="100%" height="100%"><BarChart data={data.daily} barCategoryGap="25%"><CartesianGrid vertical={false} stroke="#26352c" /><XAxis dataKey="label" axisLine={false} tickLine={false} tick={{ fill: '#93a196', fontSize: 11 }} /><YAxis hide domain={[0, 100]} /><Tooltip contentStyle={{ background: '#18231d', border: '1px solid #26352c', borderRadius: 12, color: '#f4f4e9', fontSize: 12 }} formatter={(value) => [`${value}%`, 'Completion']} /><Bar dataKey="percentage" fill="#9ed44b" radius={[5, 5, 2, 2]} maxBarSize={24} /></BarChart></ResponsiveContainer></div></div><div className="rounded-2xl border border-line bg-panel p-5 shadow-glow sm:p-6"><div className="flex items-start justify-between"><div><p className="text-xs uppercase tracking-[0.14em] text-muted">Tracked systems</p><h2 className="mt-2 font-display text-xl font-semibold text-cream">What is in motion.</h2></div><Zap size={18} className="text-amber" /></div><div className="mt-7 space-y-5"><Metric label="Tasks completed" value={data.summary.tasks ? Math.round((data.summary.completedTasks / data.summary.tasks) * 100) : 0} tone="moss" /><Metric label="Goals" value={data.summary.goalCompletion} tone="amber" /><Metric label="Skills" value={data.summary.skillProgress} tone="moss" /><Metric label="Books" value={data.summary.bookProgress} tone="amber" /></div></div></section><section className="rounded-2xl border border-line bg-panel p-5 shadow-glow sm:p-6"><div className="flex items-start justify-between"><div><p className="text-xs uppercase tracking-[0.14em] text-muted">Completion line</p><h2 className="mt-2 font-display text-xl font-semibold text-cream">Consistency is the signal.</h2></div><TrendingUp size={18} className="text-moss" /></div><div className="mt-7 h-56"><ResponsiveContainer width="100%" height="100%"><LineChart data={data.daily}><CartesianGrid vertical={false} stroke="#26352c" /><XAxis dataKey="label" axisLine={false} tickLine={false} tick={{ fill: '#93a196', fontSize: 11 }} /><YAxis hide domain={[0, 100]} /><Tooltip contentStyle={{ background: '#18231d', border: '1px solid #26352c', borderRadius: 12, color: '#f4f4e9', fontSize: 12 }} formatter={(value) => [`${value}%`, 'Completion']} /><Line type="monotone" dataKey="percentage" stroke="#e8aa55" strokeWidth={3} dot={{ r: 3, fill: '#e8aa55', strokeWidth: 0 }} /></LineChart></ResponsiveContainer></div></section></div>;
}

function Metric({ label, value, tone }) { return <div><div className="mb-2 flex justify-between text-xs"><span className="text-muted">{label}</span><span className="text-cream">{value}%</span></div><ProgressBar value={value} tone={tone} /></div>; }
